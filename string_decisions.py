text = "Now is the time..."


print(text.find("time") != -1)


print(text.startswith("Now"))

NOW = "nOw"
print(text.upper().startswith(NOW.upper()))

print(text.endswith("..."))

# str objects have many methods to use as alternatives to RegEx (Regular Expressions)
